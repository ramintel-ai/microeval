from setuptools import setup, find_packages

setup(
    name='microeval',
    version='0.1.0',
    description='Flexible, extensible framework for benchmarking micro language models (<2B params)',
    author='Your Name',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.8',
) 